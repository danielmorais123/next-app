import { Anton, Poppins } from "@next/font/google";

export const poppins = Poppins({
  weight: "400",
  subsets: ["latin"],
});

export const anton = Anton({
  weight: "400",
  subsets: ["latin"],
});